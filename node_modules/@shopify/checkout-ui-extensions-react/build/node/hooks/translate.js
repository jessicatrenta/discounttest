"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useTranslate = useTranslate;

var _react = require("react");

var _api = require("./api");

/**
 * Returns the `I18nTranslate` interface used to translate strings.
 */
function useTranslate() {
  const {
    i18n
  } = (0, _api.useExtensionApi)();
  const translate = (0, _react.useCallback)((...args) => {
    const translation = i18n.translate(...args);

    if (!Array.isArray(translation)) {
      return translation;
    }

    return translation.map((part, index) => {
      if ( /*#__PURE__*/(0, _react.isValidElement)(part)) {
        return /*#__PURE__*/(0, _react.cloneElement)(part, {
          key: index
        });
      }

      return part;
    });
  }, [i18n]);
  return translate;
}