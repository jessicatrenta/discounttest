"use strict";

var _checkoutUiExtensions = require("@shopify/checkout-ui-extensions");

(0, _checkoutUiExtensions.extend)('Checkout::Dynamic::Render', root => {
  const skeletonImage = root.createComponent(_checkoutUiExtensions.SkeletonImage, {
    inlineSize: 300,
    blockSize: 300
  });
  root.appendChild(skeletonImage);
});