import { extend, PhoneField } from '@shopify/checkout-ui-extensions';
extend('Checkout::Dynamic::Render', function (root) {
  var phoneField = root.createComponent(PhoneField, {
    label: 'Phone',
    value: '1 (555) 555-5555'
  });
  root.appendChild(phoneField);
});